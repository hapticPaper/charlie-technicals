node_modules/
.next/
# Next.js static export output directory (GitHub Pages artifact; never committed)
out/
dist/
tsconfig.tsbuildinfo

.DS_Store

# Raw snapshots are committed (append-only) so reports can be reproduced.
# Scratch analysis artifacts are ignored.
content/analysis/*
!content/analysis/.gitkeep
!content/reports/
